/**
 * The {@code ShapeType} enum is used to define the type of a {@link B2DBody}.
 * It contains the most important shapes and the setup-process as well as the
 * rendering process of {@link B2DBody} instances classifiable.
 *
 */
public enum ShapeType {
	RECT, CIRCLE, POINT, POLYGON, CHAIN, EDGE
}
